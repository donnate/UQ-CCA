% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_methods.R
\name{crossfit_cca_marginal_inference}
\alias{crossfit_cca_marginal_inference}
\title{Cross-fitted marginal feature-score regression for sparse CCA}
\usage{
crossfit_cca_marginal_inference(
  X,
  Y,
  reference_fit,
  K = 5,
  seed = 1,
  alpha = 0.05,
  fit_mode = c("fixed_lambda", "retune_cv"),
  fit_args = list(),
  ccar3_api = NULL,
  progress = interactive(),
  covariates = NULL,
  use_limma = TRUE
)
}
\arguments{
\item{X, Y}{Numeric matrices with shared rows representing the paired
samples.}

\item{reference_fit}{A fitted \code{cca_uq_fit} object, typically produced by
\code{\link[=fit_ecca_cv]{fit_ecca_cv()}} or \code{\link[=fit_ecca_fixed_lambda]{fit_ecca_fixed_lambda()}}.}

\item{K}{Number of folds used for cross-fitting.}

\item{seed}{Random seed for bootstrap resampling.}

\item{alpha}{Two-sided error level used for confidence intervals and
significance flags.}

\item{fit_mode}{Whether each fold refit reuses the reference penalty
(\code{"fixed_lambda"}) or re-runs cross-validation (\code{"retune_cv"}).}

\item{fit_args}{Optional named overrides passed to each fold refit.}

\item{ccar3_api}{Optional loaded \code{ccar3} namespace or environment.}

\item{progress}{Whether to emit bootstrap progress messages.}

\item{covariates}{Optional matrix or data frame of additional covariates to
include in every univariate regression.}

\item{use_limma}{Whether to use \code{limma} empirical-Bayes moderation when it
is available.}
}
\value{
A list containing marginal regression summaries for the X and Y
blocks, cross-fitted canonical scores, fold diagnostics, and metadata about
the inference backend.
}
\description{
Uses held-out canonical scores to estimate marginal feature associations with
the opposite-view canonical variates, optionally with a common covariate
adjustment and limma-style variance moderation.
}
