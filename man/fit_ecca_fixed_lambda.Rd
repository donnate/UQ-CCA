% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_methods.R
\name{fit_ecca_fixed_lambda}
\alias{fit_ecca_fixed_lambda}
\title{Fit sparse CCA with a fixed \code{ccar3::ecca} penalty}
\usage{
fit_ecca_fixed_lambda(
  X,
  Y,
  lambda,
  r = 2,
  preprocess_mode = c("scale", "center", "none"),
  ccar3_api = NULL,
  ccar3_path = NULL,
  prefer_source = TRUE,
  solver = "ADMM",
  LW_Sy = TRUE,
  rho = 1,
  niter = 10000,
  thresh = 1e-04,
  thresh_0 = 1e-06,
  matrix_free_threshold = 4000L,
  cg_tol = 1e-06,
  cg_maxiter = NULL,
  verbose = FALSE,
  epsilon_sv = 1e-08,
  ridge_whiten = 1e-08
)
}
\arguments{
\item{X, Y}{Numeric matrices with rows representing the shared samples and
columns representing features from the two views.}

\item{lambda}{Single penalty value passed to \code{ccar3::ecca}.}

\item{r}{Target rank, i.e. the number of canonical components to estimate.}

\item{preprocess_mode}{Preprocessing applied before fitting. \code{"scale"}
centers and scales each column, \code{"center"} only centers, and \code{"none"}
skips any extra scaling. \code{ecca} still centers internally.}

\item{ccar3_api}{Optional namespace or environment containing the \code{ccar3}
fitting functions. When \code{NULL}, the package looks for an installed
\code{ccar3} package or a local source checkout.}

\item{ccar3_path}{Optional path to a local \code{ccar3} source checkout.}

\item{prefer_source}{Whether to prefer loading \code{ccar3_path} with
\code{pkgload::load_all()} before falling back to an installed \code{ccar3}
package.}

\item{solver, LW_Sy, thresh_0, matrix_free_threshold, cg_tol, cg_maxiter}{Compatibility arguments retained in the stored settings list.}

\item{rho}{ADMM penalty parameter forwarded to \code{ccar3}.}

\item{niter}{Maximum number of iterations for the sparse CCA solver.}

\item{thresh}{Convergence tolerance passed to \code{ccar3}.}

\item{verbose}{Whether to print progress from the underlying fit.}

\item{epsilon_sv}{Small singular-value guard passed to \code{ccar3}.}

\item{ridge_whiten}{Ridge regularization used during whitening for
numerical stability.}
}
\value{
A \code{cca_uq_fit} object using the supplied \code{lambda}.
}
\description{
Fit sparse CCA with a fixed \code{ccar3::ecca} penalty
}
