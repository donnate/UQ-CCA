% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_methods.R
\name{fit_ecca_cv}
\alias{fit_ecca_cv}
\title{Fit sparse CCA with cross-validated \code{ccar3::ecca}}
\usage{
fit_ecca_cv(
  X,
  Y,
  r = 2,
  lambdas = 10^seq(-2, 0, length.out = 8),
  kfolds = 5,
  preprocess_mode = c("scale", "center", "none"),
  ccar3_api = NULL,
  ccar3_path = NULL,
  prefer_source = TRUE,
  solver = "ADMM",
  parallelize = FALSE,
  LW_Sy = TRUE,
  rho = 1,
  niter = 5000,
  thresh = 1e-04,
  thresh_0 = 1e-06,
  matrix_free_threshold = 4000L,
  cg_tol = 1e-06,
  cg_maxiter = NULL,
  verbose = FALSE,
  nb_cores = NULL,
  select = "lambda.min",
  maxiter_cv = NULL,
  set_seed_cv = NULL,
  scoring_method = c("mse", "trace"),
  cv_use_median = FALSE,
  epsilon_sv = 1e-08,
  ridge_whiten = 1e-08
)
}
\arguments{
\item{X, Y}{Numeric matrices with rows representing the shared samples and
columns representing features from the two views.}

\item{r}{Target rank, i.e. the number of canonical components to estimate.}

\item{lambdas}{Candidate penalty values passed to \code{ccar3::ecca.cv}.}

\item{kfolds}{Number of folds used by cross-validation when tuning
\code{lambda}.}

\item{preprocess_mode}{Preprocessing applied before fitting. \code{"scale"}
centers and scales each column, \code{"center"} only centers, and \code{"none"}
skips any extra scaling. \code{ecca} still centers internally.}

\item{ccar3_api}{Optional namespace or environment containing the \code{ccar3}
fitting functions. When \code{NULL}, the package looks for an installed
\code{ccar3} package or a local source checkout.}

\item{ccar3_path}{Optional path to a local \code{ccar3} source checkout.}

\item{prefer_source}{Whether to prefer loading \code{ccar3_path} with
\code{pkgload::load_all()} before falling back to an installed \code{ccar3}
package.}

\item{solver, LW_Sy, thresh_0, matrix_free_threshold, cg_tol, cg_maxiter}{Compatibility arguments retained in the stored settings list.}

\item{parallelize}{Whether to request parallel cross-validation from the
underlying \code{ccar3} routine.}

\item{rho}{ADMM penalty parameter forwarded to \code{ccar3}.}

\item{niter}{Maximum number of iterations for the sparse CCA solver.}

\item{thresh}{Convergence tolerance passed to \code{ccar3}.}

\item{verbose}{Whether to print progress from the underlying fit.}

\item{nb_cores}{Optional number of worker cores for cross-validation.}

\item{select}{Cross-validation selection rule such as \code{"lambda.min"} or
\code{"lambda.1se"} when supported by the local \code{ccar3}.}

\item{maxiter_cv}{Optional iteration cap for the inner CV fits.}

\item{set_seed_cv}{Optional seed forwarded to the \code{ccar3} CV routine.}

\item{scoring_method}{Cross-validation loss used by \code{ecca.cv}.}

\item{cv_use_median}{Whether to aggregate fold losses with the median
instead of the mean when supported by \code{ccar3}.}

\item{epsilon_sv}{Small singular-value guard passed to \code{ccar3}.}

\item{ridge_whiten}{Ridge regularization used during whitening for
numerical stability.}
}
\value{
A \code{cca_uq_fit} list containing the fitted loadings, canonical
correlations, chosen penalty, preprocessing recipe, and bookkeeping for
downstream bootstrap and cross-fitted UQ.
}
\description{
Fits sparse canonical directions on two paired data blocks and stores the
selected penalty, canonical correlations, preprocessing recipe, and feature
names needed for downstream uncertainty quantification.
}
