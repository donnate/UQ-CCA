% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_methods.R
\name{bootstrap_cca_uq}
\alias{bootstrap_cca_uq}
\title{Bootstrap uncertainty quantification for sparse CCA loadings}
\usage{
bootstrap_cca_uq(
  X,
  Y,
  reference_fit,
  B = 100,
  seed = 1,
  refit_mode = c("fixed_lambda", "retune_cv"),
  refit_args = list(),
  ccar3_api = NULL,
  progress = interactive()
)
}
\arguments{
\item{X, Y}{Numeric matrices with shared rows representing the paired
samples.}

\item{reference_fit}{A fitted \code{cca_uq_fit} object, typically produced by
\code{\link[=fit_ecca_cv]{fit_ecca_cv()}} or \code{\link[=fit_ecca_fixed_lambda]{fit_ecca_fixed_lambda()}}.}

\item{B}{Number of bootstrap replicates to request.}

\item{seed}{Random seed for bootstrap resampling.}

\item{refit_mode}{Whether each bootstrap replicate reuses the selected
penalty (\code{"fixed_lambda"}) or re-runs cross-validation (\code{"retune_cv"}).}

\item{refit_args}{Optional named overrides passed to each bootstrap refit.}

\item{ccar3_api}{Optional loaded \code{ccar3} namespace or environment.}

\item{progress}{Whether to emit bootstrap progress messages.}
}
\value{
A list with the reference fit, aligned bootstrap arrays for \code{U} and
\code{V}, aligned bootstrap canonical correlations, and replicate counts.
}
\description{
Refits sparse CCA on bootstrap resamples, aligns each bootstrap solution to
the full-data fit, and stores aligned loading arrays together with canonical
correlations for later summarization.
}
