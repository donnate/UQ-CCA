% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_methods.R
\name{crossfit_cca_inference}
\alias{crossfit_cca_inference}
\title{Cross-fitted score-loading regression with Cauchy-combined p-values}
\usage{
crossfit_cca_inference(
  X,
  Y,
  reference_fit,
  K = 5,
  seed = 1,
  alpha = 0.05,
  fit_mode = c("fixed_lambda", "retune_cv"),
  fit_args = list(),
  ccar3_api = NULL,
  progress = interactive(),
  variance_shrinkage = c("james-stein", "mr.mashr", "none"),
  mr_mashr_args = list(),
  mr_ash_args = NULL
)
}
\arguments{
\item{X, Y}{Numeric matrices with shared rows representing the paired
samples.}

\item{reference_fit}{A fitted \code{cca_uq_fit} object, typically produced by
\code{\link[=fit_ecca_cv]{fit_ecca_cv()}} or \code{\link[=fit_ecca_fixed_lambda]{fit_ecca_fixed_lambda()}}.}

\item{K}{Number of folds used for cross-fitting.}

\item{seed}{Random seed for bootstrap resampling.}

\item{alpha}{Two-sided error level used for confidence intervals and
significance flags.}

\item{fit_mode}{Whether each fold refit reuses the reference penalty
(\code{"fixed_lambda"}) or re-runs cross-validation (\code{"retune_cv"}).}

\item{fit_args}{Optional named overrides passed to each fold refit.}

\item{ccar3_api}{Optional loaded \code{ccar3} namespace or environment.}

\item{progress}{Whether to emit bootstrap progress messages.}

\item{variance_shrinkage}{Residual-variance shrinkage rule applied within
each fold.}

\item{mr_mashr_args, mr_ash_args}{Optional tuning lists forwarded to the
\code{mr.ash} / \code{mr.mashr} backend when it is requested.}
}
\value{
A list containing X-side and Y-side inference tables, fold
correlations, preprocessing diagnostics, and variance-shrinkage diagnostics.
}
\description{
This legacy cross-fitted routine regresses held-out canonical scores onto the
opposite view feature matrix one fold at a time, shrinks foldwise residual
variances, and combines fold-specific p-values with the Cauchy rule.
}
