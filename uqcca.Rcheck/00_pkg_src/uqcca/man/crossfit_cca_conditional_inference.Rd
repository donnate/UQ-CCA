% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_methods.R
\name{crossfit_cca_conditional_inference}
\alias{crossfit_cca_conditional_inference}
\title{Cross-fitted conditional regression for sparse CCA}
\usage{
crossfit_cca_conditional_inference(
  X,
  Y,
  reference_fit,
  K = 5,
  seed = 1,
  alpha = 0.05,
  fit_mode = c("fixed_lambda", "retune_cv"),
  fit_args = list(),
  ccar3_api = NULL,
  progress = interactive(),
  method = c("auto", "ols", "split_lasso"),
  selection_fraction = 0.5,
  lasso_select = c("lambda.1se", "lambda.min")
)
}
\arguments{
\item{X, Y}{Numeric matrices with shared rows representing the paired
samples.}

\item{reference_fit}{A fitted \code{cca_uq_fit} object, typically produced by
\code{\link[=fit_ecca_cv]{fit_ecca_cv()}} or \code{\link[=fit_ecca_fixed_lambda]{fit_ecca_fixed_lambda()}}.}

\item{K}{Number of folds used for cross-fitting.}

\item{seed}{Random seed for bootstrap resampling.}

\item{alpha}{Two-sided error level used for confidence intervals and
significance flags.}

\item{fit_mode}{Whether each fold refit reuses the reference penalty
(\code{"fixed_lambda"}) or re-runs cross-validation (\code{"retune_cv"}).}

\item{fit_args}{Optional named overrides passed to each fold refit.}

\item{ccar3_api}{Optional loaded \code{ccar3} namespace or environment.}

\item{progress}{Whether to emit bootstrap progress messages.}

\item{method}{Conditional regression backend: automatic selection, direct
OLS, or sample-split lasso followed by OLS on the selected features.}

\item{selection_fraction}{Fraction of samples allocated to the lasso
selection split when \code{method = "split_lasso"}.}

\item{lasso_select}{Cross-validation rule used to choose the lasso penalty.}
}
\value{
A list containing conditional regression summaries for the X and Y
blocks, cross-fitted canonical scores, fold diagnostics, and backend
details.
}
\description{
Estimates which variables in one data block conditionally predict the
opposite-view canonical score using either ordinary least squares or a
sample-split lasso plus post-selection OLS fallback in high dimensions.
}
