% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_methods.R
\name{run_alcohol_uq_comparison}
\alias{run_alcohol_uq_comparison}
\title{Run the alcohol example end-to-end}
\usage{
run_alcohol_uq_comparison(
  r = 2,
  lambdas = 10^seq(-2, 0, length.out = 6),
  kfolds = 5,
  n_boot = 25,
  K = 5,
  seed = 2025,
  preprocess_mode = c("scale", "center", "none"),
  bootstrap_refit_mode = c("fixed_lambda", "retune_cv"),
  crossfit_fit_mode = c("fixed_lambda", "retune_cv"),
  crossfit_variance_shrinkage = c("james-stein", "mr.mashr", "none"),
  mr_mashr_args = list(),
  mr_ash_args = NULL,
  data_path = NULL,
  ccar3_path = NULL,
  prefer_source = TRUE,
  parallelize = FALSE,
  nb_cores = NULL,
  alpha = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{r}{Target rank, i.e. the number of canonical components to estimate.}

\item{lambdas}{Candidate penalty values passed to \code{ccar3::ecca.cv}.}

\item{kfolds}{Number of folds used by cross-validation when tuning
\code{lambda}.}

\item{n_boot}{Number of bootstrap replicates.}

\item{K}{Number of folds used for cross-fitting.}

\item{seed}{Random seed for bootstrap resampling.}

\item{preprocess_mode}{Preprocessing applied before fitting. \code{"scale"}
centers and scales each column, \code{"center"} only centers, and \code{"none"}
skips any extra scaling. \code{ecca} still centers internally.}

\item{bootstrap_refit_mode}{Bootstrap refitting mode passed to
\code{\link[=bootstrap_cca_uq]{bootstrap_cca_uq()}}.}

\item{crossfit_fit_mode}{Foldwise refitting mode passed to the cross-fitted
inference routine.}

\item{crossfit_variance_shrinkage}{Residual-variance shrinkage rule used by
\code{\link[=crossfit_cca_inference]{crossfit_cca_inference()}}.}

\item{mr_mashr_args, mr_ash_args}{Optional tuning lists forwarded to the
\code{mr.ash} / \code{mr.mashr} backend when it is requested.}

\item{data_path}{Optional path to an \code{.rda} file containing \code{alcohol}.}

\item{ccar3_path}{Optional path to a local \code{ccar3} source checkout.}

\item{prefer_source}{Whether to prefer loading \code{ccar3_path} with
\code{pkgload::load_all()} before falling back to an installed \code{ccar3}
package.}

\item{parallelize}{Whether to request parallel cross-validation from the
underlying \code{ccar3} routine.}

\item{nb_cores}{Optional number of worker cores for cross-validation.}

\item{alpha}{Two-sided error level used for confidence intervals and
significance flags.}

\item{verbose}{Whether to print progress from the underlying fit.}
}
\value{
A list containing the loaded data, fitted sparse CCA model,
bootstrap output, cross-fitted output, and a comparison summary.
}
\description{
Convenience wrapper that loads the alcohol data, fits sparse CCA, runs the
bootstrap and cross-fitted UQ procedures, and assembles a side-by-side
summary ready for reporting.
}
