% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cca_uq_simulation.R
\name{run_rrr_uq_simulation}
\alias{run_rrr_uq_simulation}
\title{Run the reduced-rank regression simulation benchmark}
\usage{
run_rrr_uq_simulation(
  n_sims = 20,
  n = 500,
  p = 100,
  q = 20,
  r = 2,
  nnzeros = 10,
  theta_strength = "high",
  r_pca = 5,
  lambda_pca = 1,
  overlapping_amount = 1,
  normalize_diagonal = TRUE,
  n_new = 5000,
  lambdas = 10^seq(-3, 1, length.out = 30),
  kfolds = 5,
  n_boot = 25,
  K = 2,
  alpha = 0.05,
  truth_tol = 1e-08,
  bootstrap_threshold = 1e-06,
  strength_bins = 6,
  seed = 1,
  preprocess_mode = "center",
  crossfit_variance_shrinkage = c("james-stein", "mr.mashr", "none"),
  mr_mashr_args = list(),
  mr_ash_args = NULL,
  ccar3_path = NULL,
  ccar3_code_path = NULL,
  prefer_source = TRUE,
  parallelize = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{n_sims}{Number of independent simulation replications.}

\item{n}{Number of paired samples to generate.}

\item{p, q}{Numbers of features in the X and Y blocks.}

\item{r}{Rank of the latent canonical signal.}

\item{nnzeros}{Number of nonzero loadings per canonical direction in the
underlying generator.}

\item{theta_strength}{Either a named signal-strength setting (\code{"high"},
\code{"medium"}, \code{"low"}), a scalar, a length-\code{r} vector, or an \code{r}-by-\code{r}
matrix controlling the population canonical correlations.}

\item{r_pca, lambda_pca, overlapping_amount, normalize_diagonal, n_new}{Additional arguments forwarded to the external simulation generator.}

\item{lambdas}{Candidate penalties passed to \code{\link[=fit_ecca_cv]{fit_ecca_cv()}}.}

\item{kfolds}{Number of folds used by cross-validation when tuning
\code{lambda}.}

\item{n_boot}{Number of bootstrap replicates used within each simulation.}

\item{K}{Number of folds used for cross-fitting.}

\item{alpha}{Two-sided error level used for interval evaluation.}

\item{truth_tol}{Absolute threshold used to decide whether a ground-truth
coefficient counts as nonzero.}

\item{bootstrap_threshold}{Absolute threshold used when computing bootstrap
selection stability.}

\item{strength_bins}{Number of bins used when aggregating power by signal
strength.}

\item{seed}{Base random seed for the benchmark.}

\item{preprocess_mode}{Preprocessing applied before fitting. \code{"scale"}
centers and scales each column, \code{"center"} only centers, and \code{"none"}
skips any extra scaling. \code{ecca} still centers internally.}

\item{crossfit_variance_shrinkage}{Residual-variance shrinkage rule used by
the legacy cross-fitted inference routine.}

\item{mr_mashr_args, mr_ash_args}{Optional tuning lists forwarded to the
\code{mr.ash} / \code{mr.mashr} backend when it is requested.}

\item{ccar3_path}{Optional path to a local \code{ccar3} source checkout.}

\item{ccar3_code_path}{Optional path to the external \code{CCAR3_code}
repository containing the simulation scripts.}

\item{prefer_source}{Whether to prefer loading \code{ccar3_path} with
\code{pkgload::load_all()} before falling back to an installed \code{ccar3}
package.}

\item{parallelize}{Whether to request parallel cross-validation from the
underlying \code{ccar3} routine.}

\item{verbose}{Whether to print progress from the underlying fit.}
}
\value{
A list containing replication-level outcomes, summarized coverage and
false-discovery metrics, subspace recovery summaries, and the underlying
fitted objects for each simulation.
}
\description{
Repeats the synthetic-data generator, sparse CCA fit, bootstrap UQ, and
cross-fitted inference pipeline across multiple replications, then summarizes
coverage, power, false discovery, and subspace recovery.
}
